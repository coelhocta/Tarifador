# Scripts do Servidor Asterisk

Esta pasta contém os scripts que devem ser executados diretamente no servidor Asterisk.

## exporta_chamadas.py

Exporta as chamadas externas atendidas do mês anterior para um arquivo:

Mes.AA.ASTERISK.csv

Este arquivo será posteriormente processado pelo SATA.